package modules

import com.google.inject._
import play.api._

class RoutingMapModule(
    environment: Environment,
    configuration: Configuration) extends AbstractModule {
  
  def configure {
    
  }
  
}